<p id="notice"><%= notice %></p>

<h1>Meta</h1>

<table>
  <thead>
    <tr>
      <th>Idusuario</th>
      <th>Iddetalleparametro</th>
      <th>Idreto</th>
      <th>Fechaexpiracion</th>
      <th>Cantidad</th>
      <th>Reto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @meta.each do |metum| %>
      <tr>
        <td><%= metum.idusuario %></td>
        <td><%= metum.iddetalleparametro %></td>
        <td><%= metum.idreto %></td>
        <td><%= metum.Fechaexpiracion %></td>
        <td><%= metum.cantidad %></td>
        <td><%= metum.reto %></td>
        <td><%= link_to 'Show', metum %></td>
        <td><%= link_to 'Edit', edit_metum_path(metum) %></td>
        <td><%= link_to 'Destroy', metum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Metum', new_metum_path %>
